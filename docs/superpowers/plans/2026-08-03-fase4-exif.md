# Fase 4 — EXIF: Lectura y panel de información — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir lectura de metadatos EXIF (core) y un panel derecho de información (UI) que muestre un conjunto curado de campos, con carga asíncrona y cache.

**Architecture:** `core/exif.rs` define `ExifImage`, `Rational` y `read_exif` (usa `kamadak-exif`). Un worker en `app.rs` lee el EXIF en segundo plano, lo cachea por path (`ExifRead`), y el nuevo `ui/info_panel.rs` pinta un `Panel::right`. Se añade la `Action::ToggleInfo` (shortcut `I`). Todo en `core/` puro y testeable; la UI solo formatea.

**Tech Stack:** Rust, `kamadak-exif` 0.6.1, `egui`/`eframe`, `thiserror`, `serde`, `insta` (dev), `image` (dev, fixtures).

**Spec:** `docs/superpowers/specs/2026-08-03-fase4-exif-design.md`

---

### Task 1: Añadir la dependencia `kamadak-exif` y verificar el build

**Files:**
- Modify: `Cargo.toml`
- Test: `cargo check`

- [ ] **Step 1: Añadir la dependencia en `[dependencies]`**

En `Cargo.toml`, dentro de `[dependencies]` (tras `image`):

```toml
# Metadatos EXIF (JPEG/TIFF) — licencia BSD-2-Clause (AGENTS.md §7.2),
# estándar del ecosistema, recomendado por Plan.md §6.
kamadak-exif = "0.6.1"
```

Este paquete se importa en código como `kamadak_exif::...` (el `extern prelude` usa la clave del `[dependencies]`, no el `lib name` interno del crate).

- [ ] **Step 2: Verificar que compila**

Run: `cargo check`
Expected: compila sin errores (el crate aún no se usa).

- [ ] **Step 3: Commit**

```bash
git add Cargo.toml Cargo.lock
git commit -m "build: add kamadak-exif dependency for EXIF parsing"
```

---

### Task 2: Añadir la variante de error `ShImagesError::Exif`

**Files:**
- Modify: `src/utils/errors.rs` (enum + mod tests)

- [ ] **Step 1: Escribir el test que falla**

En `mod tests` de `src/utils/errors.rs`:

```rust
#[test]
fn exif_error_displays_message() {
    let err = ShImagesError::Exif("bad block".to_string());
    assert_eq!(err.to_string(), "exif error: bad block");
    assert!(matches!(err, ShImagesError::Exif(_)));
}
```

- [ ] **Step 2: Ejecutar y ver que falla**

Run: `cargo test -p sh_images --lib utils::errors`
Expected: FAIL — `ShImagesError::Exif` no existe.

- [ ] **Step 3: Añadir la variante**

En el enum `ShImagesError`, junto al resto:

```rust
/// Error parseando metadatos EXIF.
#[error("exif error: {0}")]
Exif(String),
```

- [ ] **Step 4: Ejecutar y ver que pasa**

Run: `cargo test -p sh_images --lib utils::errors`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/utils/errors.rs
git commit -m "feat(errors): add Exif error variant"
```

---

### Task 3: `core/exif.rs` — modelo, `Rational` y `read_exif` (TDD)

**Files:**
- Rewrite: `src/core/exif.rs`
- Test: `src/core/exif.rs` (mod tests)

> **Precondición (fixtures):** Copiar dos archivos pequeños (<100KB, AGENTS.md §8.2) del crate `kamadak-exif` (licencia BSD-2-Clause) a `tests/fixtures/`:
>
>   - `tests/fixtures/exif.jpg` ← `https://raw.githubusercontent.com/kamadak/exif-rs/0.6.1/tests/exif.jpg`
>   - `tests/fixtures/exif.tif` ← `https://raw.githubusercontent.com/kamadak/exif-rs/0.6.1/tests/exif.tif`
>
> Ambos contienen EXIF real (al menos Make/Model/Orientation).

- [ ] **Step 1: Escribir los tests que fallan**

Reemplazar TODO el contenido de `src/core/exif.rs` por modelo + tests (sin `read_exif` todavía):

```rust
//! Extracción de metadatos EXIF (Fase 4).
//!
//! `core/` no depende de la UI: expone `ExifImage` curado, `Rational` y
//! `read_exif`. Este módulo solo lee y mapea; la UI formatea.

use std::path::Path;

use kamadak_exif::{In, Reader, Tag, Value};

use crate::utils::errors::{Result, ShImagesError};

/// Número racional EXIF (numerador/denominador).
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Rational {
    pub num: u64,
    pub den: u64,
}

impl Rational {
    /// Crea un racional.
    pub fn new(num: u64, den: u64) -> Self {
        Self { num, den }
    }

    /// Valor decimal (n/m), o `f64::NAN` si `den == 0`.
    pub fn to_f64(self) -> f64 {
        if self.den == 0 {
            f64::NAN
        } else {
            self.num as f64 / self.den as f64
        }
    }
}

/// Metadatos curados que el panel muestra.
#[derive(Debug, Clone, PartialEq, Eq, Default)]
pub struct ExifImage {
    pub make: Option<String>,
    pub model: Option<String>,
    pub fecha: Option<String>,
    pub iso: Option<u32>,
    pub f_number: Option<Rational>,
    pub shutter_speed: Option<Rational>,
    pub focal_length: Option<Rational>,
    pub orientacion: Option<u16>,
}

/// Resultado de una lectura: distingue "sin EXIF" de "error".
#[derive(Debug, Clone, PartialEq)]
pub enum ExifRead {
    /// Metadatos leídos.
    Found(ExifImage),
    /// El archivo no tiene bloque EXIF.
    None,
    /// Error al leer/parsear.
    Error(ShImagesError),
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::path::PathBuf;

    fn fixture(name: &str) -> PathBuf {
        Path::new(env!("CARGO_MANIFEST_DIR"))
            .join("tests")
            .join("fixtures")
            .join(name)
    }

    #[test]
    fn read_exif_from_jpeg_populates_fields() {
        let img = read_exif(&fixture("exif.jpg"))
            .expect("jpg válido")
            .expect("jpg tiene EXIF");
        assert!(
            img.make.is_some() || img.model.is_some() || img.fecha.is_some(),
            "jpg con EXIF expone cámara o fecha"
        );
    }

    #[test]
    fn read_exif_from_tiff_populates_fields() {
        assert!(read_exif(&fixture("exif.tif")).expect("tif válido").is_some());
    }

    #[test]
    fn read_exif_from_png_returns_none() {
        assert_eq!(read_exif(&fixture("sample.png")).expect("ok"), None);
    }

    #[test]
    fn read_exif_from_jpeg_without_exif_returns_none() {
        assert_eq!(read_exif(&fixture("sample.jpg")).expect("ok"), None);
    }

    #[test]
    fn read_exif_corrupt_returns_error() {
        let dir = tempfile::tempdir().expect("tempdir");
        let p = dir.path().join("corrupt.jpg");
        std::fs::write(&p, b"definitely not a jpeg").expect("escribir");
        let err = read_exif(&p).expect_err("corrupto da error");
        assert!(matches!(err, ShImagesError::Exif(_) | ShImagesError::Io(_)));
    }

    #[test]
    fn rational_turns_decimal() {
        assert_eq!(Rational::new(1, 2).to_f64(), 0.5);
        assert!(Rational::new(1, 0).to_f64().is_nan());
    }

    #[test]
    fn is_exif_capable_checks_extensions() {
        assert!(is_exif_capable(Path::new("a.JPG")));
        assert!(is_exif_capable(Path::new("a.TIFF")));
        assert!(!is_exif_capable(Path::new("a.PNG")));
    }
}
```

- [ ] **Step 2: Ejecutar y ver los errores de compilación esperados**

Run: `cargo test -p sh_images --lib core::exif`
Expected: FAIL — `read_exif`, `is_exif_capable` no definidos.

- [ ] **Step 3: Implementar `is_exif_capable` + helpers + `build_image` + `read_exif`**

Añadir tras el item `ExifRead` (antes del `mod tests`):

```rust
/// Los formatos que pueden llevar EXIF en esta fase.
fn is_exif_capable(path: &Path) -> bool {
    path.extension()
        .and_then(|e| e.to_str())
        .map(|e| matches!(e.to_ascii_lowercase().as_str(), "jpg" | "jpeg" | "tif" | "tiff"))
        .unwrap_or(false)
}

/// Lee un campo ASCII (primer valor), p. ej. Make/Model/DateTime.
///
/// `Value::Ascii` es `Vec<Vec<u8>>`; se toma el primer elemento y se limpia.
fn ascii_value(exif: &kamadak_exif::Exif, tag: Tag) -> Option<String> {
    let field = exif.get_field(tag, In::PRIMARY)?;
    if let Value::Ascii(v) = &field.value {
        v.first().map(|b| String::from_utf8_lossy(b).trim().to_string())
    } else {
        None
    }
}

/// Lee un RATIONAL (primer valor) como `Rational`, si el tag es de ese tipo.
fn rational_value(exif: &kamadak_exif::Exif, tag: Tag) -> Option<Rational> {
    let field = exif.get_field(tag, In::PRIMARY)?;
    if let Value::Rational(v) = &field.value {
        v.first().map(|r| Rational::new(r.num.into(), r.denom.into()))
    } else {
        None
    }
}

/// Lee un entero sin signo (BYTE/SHORT/LONG), p. ej. Orientation.
fn uint_value(exif: &kamadak_exif::Exif, tag: Tag) -> Option<u32> {
    exif.get_field(tag, In::PRIMARY)?.value.get_uint(0)
}

/// ISO: algunos archivos usan `PhotographicSensitivity`, otros `ISOSpeed`.
fn iso_value(exif: &kamadak_exif::Exif) -> Option<u32> {
    uint_value(exif, Tag::PhotographicSensitivity).or_else(|| uint_value(exif, Tag::ISOSpeed))
}

fn build_image(exif: &kamadak_exif::Exif) -> ExifImage {
    ExifImage {
        make: ascii_value(exif, Tag::Make),
        model: ascii_value(exif, Tag::Model),
        fecha: ascii_value(exif, Tag::DateTimeOriginal)
            .or_else(|| ascii_value(exif, Tag::DateTime)),
        iso: iso_value(exif),
        f_number: rational_value(exif, Tag::FNumber),
        shutter_speed: rational_value(exif, Tag::ExposureTime),
        focal_length: rational_value(exif, Tag::FocalLength),
        orientacion: uint_value(exif, Tag::Orientation).map(|v| v as u16),
    }
}

/// Lee los metadatos EXIF de una imagen.
///
/// `Ok(Some(img))` si hay EXIF; `Ok(None)` si el archivo es de un tipo sin
/// EXIF (PNG/BMP/WebP) o es un JPEG/TIFF sin bloque EXIF; `Err` si no se puede
/// leer el archivo o su EXIF es corrupto.
pub fn read_exif(path: &Path) -> Result<Option<ExifImage>, ShImagesError> {
    if !is_exif_capable(path) {
        return Ok(None);
    }
    let file = std::fs::File::open(path)?;
    let exif = match Reader::new().read_from_container(&mut std::io::BufReader::new(file)) {
        Ok(exif) => exif,
        // JPEG/TIFF sin bloque APP1 → el crate reporta NotFound → "sin EXIF".
        Err(kamadak_exif::Error::NotFound(_)) => return Ok(None),
        Err(other) => return Err(ShImagesError::Exif(other.to_string())),
    };
    Ok(Some(build_image(&exif)))
}
```

> API confirmada (0.6.1): `Value` se matchea por variante (`Value::Ascii(Vec<Vec<u8>>)`, `Value::Rational(Vec<Rational>)`, campos `Rational { num: u32, denom: u32 }`); `Value::get_uint(i)` es público; `Field::value` es campo público. NO uses `Value::ascii()`/`Value::rational()` (son `pub(crate)` de la librería, no accesibles).

- [ ] **Step 4: Ejecutar los tests finales del módulo**

Segine `use super::*;` y `use std::path::{Path, PathBuf};` son compatibles (el `Path` importado arriba en el crate y en tests). Si colisionan, en el `mod tests` usa `Path` desde `super` o califica. Corregir hasta que pase.

Run: `cargo test -p sh_images --lib core::exif`
Expected: PASS. Si algún fixture no expone exactamente make/modelo, ajusta la aserción a lo que el archivo real devuelva (no rompa el test, mide el comportamiento real).

- [ ] **Step 5: Commit**

```bash
git add src/core/exif.rs tests/fixtures/exif.jpg tests/fixtures/exif.tif
git commit -m "feat(exif): parse curated EXIF (ExifImage, Rational, read_exif)"
```

---

### Task 4: `Action::ToggleInfo`, `KeyCode::KeyI` y plumbing

**Files:**
- Modify: `src/core/actions.rs`
- Modify: `src/core/shortcuts.rs`
- Modify: `src/ui/shortcut_dialog.rs`
- Modify: `src/ui/toolbar.rs`

- [ ] **Step 1: Añadir `KeyCode::KeyI` en `shortcuts.rs`**

En el enum `KeyCode`, junto a `KeyH`:

```rust
    KeyI,
```

En `KeyCode::to_str` añadir:

```rust
            KeyCode::KeyI => "I",
```

En `KeyCode::from_str` añadir:

```rust
            "I" => Some(KeyCode::KeyI),
```

- [ ] **Step 2: Añadir `Action::ToggleInfo` en `actions.rs`**

- Añadir `ToggleInfo` al enum.
- `label()`: `Action::ToggleInfo => "Mostrar/ocultar información",`
- `default_shortcut()`: `Action::ToggleInfo => KeyBinding::new(KeyCode::KeyI, Modifiers::None),`
- `all()`: array pasa a tamaño 11 y añade `Action::ToggleInfo`.

- [ ] **Step 3: Actualizar tests de `actions.rs`**

- `all_returns_exactly_ten_actions` → renombrar `all_returns_eleven_actions`, 10 → 11.
- En `label_is_stable_and_descriptive` añadir `assert_eq!(Action::ToggleInfo.label(), "Mostrar/ocultar información");`.

- [ ] **Step 4: Actualizar tests de `shortcuts.rs`**

- `defaults_has_one_entry_per_action`: `map.iter().count() == 10` → `11`.
- `default_shortcuts_match_spec`: añadir `assert_eq!(map.get(Action::ToggleInfo).unwrap().to_string(), "I");`.

- [ ] **Step 5: Mapear `I` en `shortcut_dialog.rs`**

En `keybinding_from_egui`, antes del `_ => return None`:

```rust
        egui::Key::I => KeyCode::KeyI,
```

- [ ] **Step 6: Botón en `toolbar.rs`**

Tras el bloque de `ToggleSidebar`:

```rust
            if toolbar_button(ui, "Info", Action::ToggleInfo, shortcuts) {
                clicked = Some(Action::ToggleInfo);
            }
```

- [ ] **Step 7: Ejecutar + regenerar snapshots**

Run: `cargo test -p sh_images --lib core::actions core::shortcuts`
Expected: FALLAN `snapshot_default_shortcuts_map` y `snapshot_default_keybinding_strings` por el atajo nuevo.

Regenerar y revisar antes de commit:

```bash
$env:INSTA_UPDATE="always"; cargo test -p sh_images --lib core::shortcuts
```

Revisa el diff en `src/core/snapshots/`; solo debe aparecer la fila `ToggleInfo`/`I`.

- [ ] **Step 8: Commit**

```bash
git add src/core/actions.rs src/core/shortcuts.rs src/ui/shortcut_dialog.rs src/ui/toolbar.rs src/core/snapshots
git commit -m "feat: add ToggleInfo action (shortcut I) to actions, shortcuts, toolbar"
```

---

### Task 5: `ui/info_panel.rs` — formateo puro + panel derecho

**Files:**
- Create: `src/ui/info_panel.rs`
- Modify: `src/ui/mod.rs`

- [ ] **Step 1: Crear el módulo con formateo puro**

```rust
//! Panel derecho con la información EXIF de la imagen actual.
//!
//! Formatea `ExifImage` en filas campo:valor y pinta un `Panel::right`
//! desplazable. La lectura (I/O) la orquesta `app.rs`; aquí solo se muestra.

use std::collections::HashMap;
use std::path::{Path, PathBuf};
use std::sync::Mutex;

use eframe::egui;

use crate::core::exif::{ExifImage, ExifRead, Rational};

/// Estado del panel: solo su visibilidad; el contenido se lee del cache.
#[derive(Debug)]
pub struct InfoPanelState {
    pub show: bool,
}

impl Default for InfoPanelState {
    fn default() -> Self {
        Self { show: true }
    }
}

#[derive(Debug, Clone, Copy)]
enum Fmt {
    Aperture,
    Shutter,
    Focal,
}

/// Formatea un `Rational` según su tipo de campo.
fn format_rational(r: Rational, fmt: Fmt) -> String {
    let d = r.to_f64();
    match fmt {
        Fmt::Aperture => format!("f/{d:.1}"),
        Fmt::Shutter => {
            if r.den != 0 && r.num != 0 {
                format!("1/{} s", (r.den as f64 / r.num as f64).round())
            } else {
                format!("{d} s")
            }
        }
        Fmt::Focal => format!("{d:.0} mm"),
    }
}

fn orientation_label(o: u16) -> String {
    match o {
        1 => "Horizontal".to_string(),
        3 | 6 | 8 => format!("Rotada ({o})"),
        other => format!("Vertical ({other})"),
    }
}

/// Convierte `ExifImage` en filas `(etiqueta, valor)`; omite los ausentes.
pub fn field_rows(img: &ExifImage) -> Vec<(String, String)> {
    let mut rows = Vec::new();
    if let Some(v) = &img.make {
        rows.push(("Fabricante".into(), v.clone()));
    }
    if let Some(v) = &img.model {
        rows.push(("Modelo".into(), v.clone()));
    }
    if let Some(v) = &img.fecha {
        rows.push(("Fecha".into(), v.clone()));
    }
    if let Some(v) = img.iso {
        rows.push(("ISO".into(), v.to_string()));
    }
    if let Some(v) = img.f_number {
        rows.push(("Apertura".into(), format_rational(v, Fmt::Aperture)));
    }
    if let Some(v) = img.shutter_speed {
        rows.push(("Obturador".into(), format_rational(v, Fmt::Shutter)));
    }
    if let Some(v) = img.focal_length {
        rows.push(("Focal".into(), format_rational(v, Fmt::Focal)));
    }
    if let Some(o) = img.orientacion {
        rows.push(("Orientación".into(), orientation_label(o)));
    }
    rows
}

/// Pinta el panel derecho. Lee del cache compartido; `None` → "Cargando…".
pub fn show(
    ui: &mut egui::Ui,
    cache: &Mutex<HashMap<PathBuf, ExifRead>>,
    current: &Path,
) {
    let entries = cache.lock().unwrap_or_else(|p| p.into_inner());
    egui::Panel::right("info")
        .default_size(220.0)
        .resizable(true)
        .show(ui, |ui| {
            ui.heading("Información");
            egui::ScrollArea::vertical().show(ui, |ui| {
                match entries.get(current) {
                    Some(ExifRead::Found(img)) => {
                        let rows = field_rows(img);
                        if rows.is_empty() {
                            ui.label("Sin datos");
                        } else {
                            egui::Grid::new("info_grid")
                                .num_columns(2)
                                .show(ui, |ui| {
                                    for (k, v) in rows {
                                        ui.label(k);
                                        ui.label(v);
                                        ui.end_row();
                                    }
                                });
                        }
                    }
                    Some(ExifRead::None) => {
                        ui.label("Sin metadatos EXIF");
                    }
                    Some(ExifRead::Error(_)) => {
                        ui.label("No se pudo leer los metadatos");
                    }
                    None => {
                        ui.label("Cargando…");
                    }
                }
            });
        });
}
```

- [ ] **Step 2: Tests de formateo puro**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    fn sample() -> ExifImage {
        ExifImage {
            make: Some("Nikon".into()),
            model: Some("D750".into()),
            fecha: Some("2020:01:02 03:04:05".into()),
            iso: Some(400),
            f_number: Some(Rational::new(28, 10)),
            shutter_speed: Some(Rational::new(1, 125)),
            focal_length: Some(Rational::new(50, 1)),
            orientacion: Some(6),
        }
    }

    #[test]
    fn row_is_omitted_when_field_absent() {
        let rows = field_rows(&ExifImage::default());
        assert!(rows.is_empty());
    }

    #[test]
    fn present_fields_become_rows() {
        let rows = field_rows(&sample());
        assert_eq!(rows.len(), 8);
        assert_eq!(rows[0], ("Fabricante".into(), "Nikon".into()));
    }

    #[test]
    fn rational_formatting_is_human_readable() {
        assert_eq!(format_rational(Rational::new(50, 1), Fmt::Focal), "50 mm");
        assert_eq!(format_rational(Rational::new(28, 10), Fmt::Aperture), "f/2.8");
    }

    #[test]
    fn snapshot_field_rows() {
        insta::assert_debug_snapshot!(field_rows(&sample()));
    }
}
```

Run: `cargo test -p sh_images --lib ui::info_panel`
Expected: FALLAN los primeros run por el snapshot nuevo (crea archivo `.snap`.new). Revísalo y acéptalo:

```bash
$env:INSTA_UPDATE="always"; cargo test -p sh_images --lib ui::info_panel
```

Luego `cargo test -p sh_images --lib ui::info_panel` → PASS.

- [ ] **Step 3: Registrar el módulo**

En `src/ui/mod.rs` añadir `pub mod info_panel;`.

- [ ] **Step 4: Commit**

```bash
git add src/ui/info_panel.rs src/ui/mod.rs src/ui/snapshots
git commit -m "feat(ui): add informational EXIF right panel"
```

---

### Task 6: Wiring en `app.rs` — worker EXIF, cache, dispatch y render

**Files:**
- Modify: `src/app.rs`

- [ ] **Step 1: Imports y campos**

Junto a los imports existentes, añadir:

```rust
use std::collections::{HashMap, HashSet};
use std::path::{Path, PathBuf};
use crate::core::exif::{read_exif, ExifRead};
use crate::ui::info_panel::{self, InfoPanelState};
```

Añadir campos a `ShImagesApp`:

```rust
    /// Cache de EXIF por path (se limpia al abrir una carpeta).
    exif_cache: Arc<Mutex<HashMap<PathBuf, ExifRead>>>,
    /// Emisor de peticiones de EXIF (UI → worker).
    exif_tx: mpsc::Sender<PathBuf>,
    /// Receptor de avisos "EXIF listo" (solo dispara repaint).
    exif_rx: Option<mpsc::Receiver<()>>,
    /// Estado del panel derecho de info.
    info_panel: InfoPanelState,
```

- [ ] **Step 2: Inicializar worker EXIF en `new`**

Antes de `Self { ... }`, tras el pool de miniaturas, añadir:

```rust
        let (exif_tx, exif_rx) = mpsc::channel::<PathBuf>();
        let (exif_events_tx, exif_events_rx) = mpsc::channel::<()>();
        let exif_cache = Arc::new(Mutex::new(HashMap::new()));
        {
            let cache = exif_cache.clone();
            let events_tx = exif_events_tx.clone();
            let ctx = ctx.clone();
            std::thread::spawn(move || {
                while let Ok(path) = exif_rx.recv() {
                    let result = match read_exif(&path) {
                        Ok(Some(img)) => ExifRead::Found(img),
                        Ok(None) => ExifRead::None,
                        Err(e) => ExifRead::Error(e),
                    };
                    if let Ok(mut m) = cache.lock() {
                        m.insert(path, result);
                    }
                    if events_tx.send(()).is_err() {
                        tracing::debug!("exif event dropped (receiver gone)");
                    }
                    ctx.request_repaint();
                }
            });
        }
```

Y en `Self { ... }` añadir los campos:

```rust
            exif_cache,
            exif_tx,
            exif_rx: Some(exif_events_rx),
            info_panel: InfoPanelState::default(),
```

(El worker consume del `exif_rx` local de la función; la app conserva `exif_tx` para enviar peticiones y `exif_events_rx` para las señales.)

- [ ] **Step 3: Métodos `request_exif` y `poll_exif`**

```rust
    /// Encolar una petición de EXIF para `path` si aún no está cacheado.
    fn request_exif(&self, path: &Path) {
        let present = self
            .exif_cache
            .lock()
            .map(|m| m.contains_key(path))
            .unwrap_or(false);
        if present {
            return;
        }
        let _ = self.exif_tx.send(path.to_path_buf());
    }

    /// Drena las señales "EXIF listo" y hace repaint para repintar el panel.
    fn poll_exif(&mut self) {
        let Some(rx) = self.exif_rx.take() else { return };
        let mut repaint = false;
        while rx.try_recv().is_ok() {
            repaint = true;
        }
        self.exif_rx = Some(rx);
        if repaint {
            self.ctx.request_repaint();
        }
    }
```

- [ ] **Step 4: Llamar al ciclo de vida**

- En `apply_decoded` (tras `self.preload_neighbors();`): `self.request_exif(path);`
- En `open_path`, tras limpiar `thumb_cache`/texturas: `self.exif_cache.lock().map(|mut m| m.clear());`
- En `ui()`, tras `self.poll_thumbnails();`: `self.poll_exif();`
- En `dispatch`, añadir arm:
  ```rust
  Action::ToggleInfo => self.info_panel.show = !self.info_panel.show,
  ```

- [ ] **Step 5: Render del panel en `ui()`**

Tras el bloque del sidebar (y solo con imagen), añadir:

```rust
        if self.texture.is_some() && self.info_panel.show {
            if let Some(nav) = &self.navigation {
                if let Some(path) = nav.current_path() {
                    info_panel::show(ui, &self.exif_cache, path);
                }
            }
        }
```

- [ ] **Step 6: `cargo check` + `clippy` limpio**

Run: `cargo check` y `cargo clippy -- -D warnings`
Expected: limpio. Resuelve imports/tipos hasta que no haya warnings/errors.

- [ ] **Step 7: Commit**

```bash
git add src/app.rs src/ui/info_panel.rs
git commit -m "feat(app): wire EXIF worker, cache and right info panel"
```

---

### Task 7: Test de integración del flujo EXIF

**Files:**
- Modify: `tests/integration.rs`
- Modify: `tests/common/mod.rs`

- [ ] **Step 1: Helper `copy_fixture` en `tests/common/mod.rs`**

Al final del archivo:

```rust
/// Copia un fixture committeado a un directorio temp y devuelve su ruta.
pub fn copy_fixture(dir: &Path, name: &str) -> PathBuf {
    let src = Path::new(env!("CARGO_MANIFEST_DIR"))
        .join("tests")
        .join("fixtures")
        .join(name);
    let dst = dir.join(name);
    fs::copy(&src, &dst).expect("copiar fixture");
    dst
}
```

Asegura el import `Path` ya existe en ese módulo (sí).

- [ ] **Step 2: añadir el flujo en `tests/integration.rs`**

Añadir imports relevantes y el test:

```rust
use sh_images::core::exif::read_exif;
use common::copy_fixture;

/// Flujo 7 — EXIF: leer metadatos de un JPEG real; no-crash sin EXIF.
#[test]
fn flujo_exif() {
    let dir = tempfile::tempdir().expect("tempdir");

    let jpg = copy_fixture(dir.path(), "exif.jpg");
    let img = read_exif(&jpg).expect("leer ok").expect("jpg con exif");
    assert!(
        img.make.is_some() || img.model.is_some() || img.fecha.is_some(),
        "JPEG con EXIF expone cámara o fecha"
    );

    let png = copy_fixture(dir.path(), "sample.png");
    assert_eq!(read_exif(&png).expect("ok"), None, "PNG sin EXIF → None");
}
```

Run: `cargo test -p sh_images --test integration flujo_exif`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add tests/integration.rs tests/common/mod.rs
git commit -m "test: add integration flow for EXIF reading"
```

---

### Task 8: Documentar ADR y CHANGELOG

**Files:**
- Modify: `docs/ARCHITECTURE.md`
- Create: `CHANGELOG.md` (si no existe) o append

- [ ] **Step 1: ADR-009 en `docs/ARCHITECTURE.md`**

Append:

```markdown
## ADR-009: Metadatos EXIF con `kamadak-exif` y panel de información

- **Contexto:** Fase 4 necesita leer EXIF (JPEG/TIFF) y mostrarlo; `core/exif.rs`
  era un stub. La lectura es I/O y no debe bloquear el UI thread (AGENTS.md §7.1).
- **Decisión:** `kamadak-exif` 0.6.1 (BSD-2-Clause, estándar, Plan.md §6) en
  `core/exif.rs` con un modelo curado `ExifImage` (Make, Model, fecha, ISO,
  f-number, ExposureTime, FocalLength, Orientation) y `Rational`. Un worker de
  `app.rs` lee el EXIF en segundo plano y lo cachea por path (`ExifRead`
  distingue Found/None/Error). Un nuevo `ui::info_panel` pinta un `Panel::right`
  con las filas formateadas; `Action::ToggleInfo` (shortcut `I`) lo alterna.
- **Consecuencias:** La UI no bloquea al leer EXIF grande; el cache por path
  evita re-parseos; imágenes sin EXIF muestran "Sin metadatos"; errores → toast.
  Un JPEG/TIFF sin bloque APP1 hace que la librería devuelva `Error::NotFound`,
  que se mapea a `Ok(None)` (no es un error).
- **Alternativas:** `exif` (menos mantenido), parser manual del APP1/TIFF
  (frágil), lectura síncrona en el UI thread (viola §7.1).
```

- [ ] **Step 2: CHANGELOG**

Crear (o actualizar) `CHANGELOG.md` con una entrada "Fase 4 — Metadatos EXIF" listando la lectura EXIF y el panel de información.

- [ ] **Step 3: Commit**

```bash
git add docs/ARCHITECTURE.md CHANGELOG.md
git commit -m "docs: ADR-009 EXIF + changelog Fase 4"
```

---

### Task 9: QA final (fmt, check, clippy, test, release)

**Files:** ninguno

- [ ] **Step 1: Checklist AGENTS.md §5.1**

```bash
cargo fmt
cargo fmt --check
cargo clippy -- -D warnings
cargo test
cargo test --release
```

Resuelve cualquier warning/fallo. Si algún snapshot (shortcuts/field_rows) quedó fuera por plataforma, regenera con `INSTA_UPDATE=always` y revisa el diff.

- [ ] **Step 2: sin `.unwrap()`/`.expect()` en producción**

```bash
rg -n "\.unwrap\(\)|\.expect\(" src
```

Permitidos: dentro de `#[cfg(test)]` y el patrón `unwrap_or_else(|p| p.into_inner())` para locks (ya existente). `request_exif` usa `.unwrap_or(false)`, no panic.

- [ ] **Step 3: QA manual (git-branchmente documentado)**

`cargo run` — abrir una imagen JPG con EXIF → teclear `I` → panel derecho muestra Make/Modelo/Fecha/ISO/Apertura. Abrir un PNG → "Sin metadatos EXIF". Documentar en el PR.

- [ ] **Step 4: Commit final**

```bash
git add -A
git commit -m "chore: final fmt/clippy pass for Fase 4 EXIF"
```

---

## Notas para el ejecutor

- **Import del crate:** `kamadak-exif` se referencia como `kamadak_exif::…`. `Tag`, `In`, `Value`, `Reader`, `Error` se re-exportan del raíz.
- **API value:** `Field::value` es público; matchea `Value::Ascii(Vec<Vec<u8>>)` y `Value::Rational(Vec<Rational>)`; `Rational { num: u32, denom: u32 }`. `Value::get_uint(i) -> Option<u32>`.
- **ISO:** algunos archivos usan `PhotographicSensitivity`, otros `ISOSpeed`; `iso_value` intenta ambos (order).
- **JPEG sin EXIF →** `read_from_container` devuelve `Error::NotFound`; se mapea a `Ok(None)`.
- **Snapshots:** regenerar con `INSTA_UPDATE=always` y revisar el diff (nunca aceptar ciegamente).
- **Aserción de orientación:** depende del fixture copiado; usa aserción genérica de "algún campo cámara/fecha presente" para no acoplarse a un valor concreto.